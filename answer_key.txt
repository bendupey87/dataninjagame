question 1:
import pandas as pd
df =pd.read_csv('/tmp/mystic_coffee_sales.csv')
df.head()

question 2:
df['date']=pd.to_datetime(df['date'])

question 3:
df['month']=df['date'].dt.to_period('M').astype(str)

question 4:
totals = df.groupby('drink')['revenue'].sum()

question 5:
totals.sort_values(ascending=False).head(5).plot(kind='bar')
import matplotlib.pyplot as plt
plt.show()
